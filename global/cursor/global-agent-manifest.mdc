---
description: 跨 Agent 全局规范入口 — Cursor / Claude Code / Codex / VSCode Copilot 路径与优先级
alwaysApply: true
---

# 跨 Agent 规范入口

## 规则路径（优先级：项目 > 全局）

| Agent | 全局规则 | 项目规则 |
|-------|----------|----------|
| **Cursor** | `~/.cursor/rules/global-*.mdc` | `<repo>/.cursor/rules/*.mdc`、`<repo>/AGENTS.md` |
| **Claude Code** | `~/.claude/CLAUDE.md` + 读 `~/.cursor/rules/global-*.mdc` | `<repo>/AGENTS.md`、`<repo>/CLAUDE.md`、`<repo>/.cursor/rules/` |
| **Codex** | `~/.codex/AGENTS.md` | `<repo>/AGENTS.md` |
| **VSCode Copilot** | `~/.vscode/copilot-instructions.md` + settings.json | `<repo>/.github/copilot-instructions.md` |

入口模板权威源：`~/Documents/code/rule/agents/`（`claude-CLAUDE.md` / `codex-AGENTS.md` / `vscode-copilot-instructions.md`），由同步脚本部署。

## Skill 路径

| 角色 | 路径 |
|------|------|
| **codeskill 权威源** | `~/Documents/code/skill/codeskill/<name>/` |
| **ui-skills 权威源** | `~/Documents/code/skill/ui-skills/skills/<name>/` |
| **运行时 canonical** | `~/.claude/skills/<name>/` |
| **Git 全量镜像** | `~/Documents/code/skill/global/<name>/` |
| **场景路由** | `~/Documents/code/skill/SKILL_ROUTER.md` |
| **全量目录** | `~/Documents/code/skill/SKILLS_INDEX.md` |

| Agent | 读取路径 |
|-------|----------|
| **Cursor** | `~/.cursor/skills/`（symlink） |
| **Claude Code** | `~/.claude/skills/<name>/SKILL.md` |
| **Codex** | `~/.codex/skills/`（symlink） |
| **VSCode Copilot** | `~/.vscode/skills/`（symlink） |
| **Agents 镜像** | `~/.agents/skills/`（symlink） |

安装/同步：

```bash
~/Documents/code/rule/sync-global-agent-standards.sh   # codeskill + ui-skills + skills + global镜像 + rules + agents入口
~/Documents/code/rule/sync-global-commands.sh          # slash 命令
```

项目级 Copilot 指令部署：

```bash
~/.vscode/deploy-copilot.sh <project_dir>   # 拷贝完整指令到 <project_dir>/.github/copilot-instructions.md
```

## 本项目沉淀的规范 Skill

| Skill | 用途 |
|-------|------|
| `preview-first-sync` | 预览先展示、异步入库 |
| `shadcn-app-components` | 组件三层 + 模板 |
| `framer-motion-patterns` | lib/motion.ts 动效 |
| `nextjs-saas-feature-scaffold` | 新 feature 脚手架 |
| `saas-tenant-membership` | 租户成员关系 |
| `evolve-skills` | 全局 skill 自进化（纠正 / 踩坑写回） |
| `prod-error-layered-triage` | 生产报错四层分流（缓存/env/数据/构建） |
| `runtime-config-source-of-truth` | 运行时配置真相来源 + 多环境实例级覆盖 |
| `encrypted-credential-cross-env-migration` | 加密凭据跨库迁移四步套路 |
| `local-shell-tooling-portability` | macOS bash 3.2 / zsh / 代理脚本可移植性 |

## UI Skills（ibelick/ui-skills）

| Skill | 用途 |
|-------|------|
| `ui-skills-root` | UI 任务路由入口（或 `npx ui-skills start`） |
| `baseline-ui` | 快速去 AI 味 / 间距层级 |
| `improve-ui` | 审计界面并写实现计划 |
| `create-design-md` | 生成 DESIGN.md |
| `fixing-accessibility` / `fixing-metadata` / `fixing-motion-performance` | 专项修复 |

本机备份：`~/Documents/code/skill/ui-skills/`。细则：`global-ui-skills.mdc`。

## Interface Skills（jakubkrehel/skills）

| Skill | 用途 |
|-------|------|
| `better-interface` | 跨 a11y·布局·文案·字·色·UI 一次审查 |
| `better-ui` / `better-typography` / `better-colors` | UI 细节 / 排版 / 色系统 |
| `better-layout` / `better-accessibility` / `better-writing` | 布局 / a11y 细则 / 产品文案 |
| `interface-review` / `break` / `variant` / `explain-interface` | 变更审查 / 压测 / 变体 / 逆向解释（用户点名） |

安装：`npx skills add jakubkrehel/skills -g -y` → `~/.agents/skills/` → sync bridge → `global/`。细则：`global-ui-skills.mdc`。

## MCP（跨 Agent）

| 项 | 路径 |
|----|------|
| Canonical 备份 | `~/Documents/code/rule/mcp/canonical.json` |
| 同步脚本 | `~/Documents/code/rule/sync-global-mcp.sh` |
| 说明 | `~/Documents/code/rule/mcp/README.md` |

部署目标：Cursor / Claude Code / Codex / VS Code Copilot。新增服务器写进 `canonical.json` 后跑同步即可（密钥走 `secrets.local.json`）。已含 `chrome-devtools`（[ChromeDevTools/chrome-devtools-mcp](https://github.com/ChromeDevTools/chrome-devtools-mcp)）、`context-mode`（[mksglu/context-mode](https://github.com/mksglu/context-mode)）。

## Context Mode（mksglu/context-mode）

| Skill | 用途 |
|-------|------|
| `context-mode` | 大工具输出走 `ctx_execute` / 沙箱，省 context |
| `ctx-doctor` / `ctx-stats` / `ctx-index` / `ctx-search` | 诊断 / 统计 / 索引 / 搜索（L3） |

已卸：`context-mode-ops` · `ctx-insight` · `ctx-purge` · `ctx-upgrade`。MCP：`mcp/canonical.json` → `context-mode`。

## Marketing Skills（coreyhaines31/marketingskills）

| Skill | 用途 |
|-------|------|
| `product-marketing` | 新项目营销入口（ICP / 定位） |
| `marketing-plan` / `marketing-ideas` / `marketing-loops` | 计划 / 点子 / 循环自动化 |
| `copywriting` / `cro` / `seo-audit` | 文案 / 转化 / SEO（可自动） |
| 短名渠道（`ads`/`image`/`launch`/…） | **L3 点名**，防工程任务误触 |

安装：`npx skills add coreyhaines31/marketingskills -g -y` → bridge → `global/`。细则：`SKILL_ROUTER.md` Marketing 节。

## 精简策略（2026-09）

- 已卸：Remotion 全家桶、`find-skills`、context-mode 运维型 skill
- 已卸：Matt/grill/caveman 全家桶（`ask-matt`/`implement`/`handoff`/`grilling`/`caveman*`/`cavecrew`/…）
- 已卸：`marketing-council` · `directory-submissions`
- UI 默认栈固定；大改版视觉 skill → L3
- 营销短名 → L3

## 全局规则清单

- `global-skills-evolution.mdc` — Skills 自进化权威源与同步
- `global-agent-workflow.mdc` — 工作流 + skill 路由（自动触发）
- `global-codex-project-structure.mdc` — Codex / Agent 项目脚手架结构
- `global-nextjs-saas-architecture.mdc` — 架构约束
- `global-security-baseline.mdc` — 安全基线
- `global-ui-conventions.mdc` — UI 交互
- `global-ui-skills.mdc` — ibelick/ui-skills 路由 + jakubkrehel L2（code 下全项目）
- `global-motion-conventions.mdc` — 动效约束
- `global-data-patterns.mdc` — 缓存模式指针
- `global-interaction-performance.mdc` — 交互性能（伪导航、DB 合并、乐观 UI）## 个人知识库（agent_KB）

所有智能体会话均挂载同一知识库（唯一路径，无 symlink / 旧名兼容）：

- 根目录：`/Users/marui/Documents/code/agent_KB`
- 协议：`/Users/marui/Documents/code/agent_KB/AGENTS.md`（**唯一行为源**）
- 默认可写：`inbox/`；**通用结论**经抽象后同轮可自动升 `memory/`；新建 playbook / profile / 项目级仍须确认
- 业务仓：`.cursor/rules/agent_KB-pointer.mdc` + `AGENTS.md` 绑定 `projects/<repo>.md`
- 查询：先读 `_meta/index.md`；有价值答案**回写 inbox**
- 健康检查：触发语「体检 wiki」→ `_meta/lint` / `playbooks/wiki-lint`
- 源材料：`raw/` 只读；编译进 wiki
- **新建 / 打开未挂接的 `~/Documents/code/<repo>`**：立即跑  
  `~/Documents/code/agent_KB/scripts/hook-project.sh <dir>`  
  （用 `_templates/project-agents.md` 创建完整 `AGENTS.md`，不必先问用户）
- Cursor 自动挂接：`workspaceOpen` / `sessionStart` hook（安装见下）
- 工具备份与手动安装：`~/Documents/code/rule/agent_KB/README.md`

```bash
# 挂接单个项目
~/Documents/code/agent_KB/scripts/hook-project.sh ~/Documents/code/<repo>

# 安装 Cursor 自动创建 AGENTS.md（也可由 sync 脚本部署）
~/Documents/code/agent_KB/scripts/install-cursor-auto-hook.sh

# 全量同步（镜像备份到 rule/ + 部署各 Agent 入口 + Cursor hook）
~/Documents/code/rule/sync-global-agent-standards.sh
```

## 会话启动

0. **按用户意图自动读对应 `SKILL.md`**（先 `SKILL_ROUTER.md`「自动调用协议」 / `using-agent-skills`），不必等用户点名；**全面 = 路由覆盖全部已装 skill**，同轮只 Read 1 个 L1（+ 可选门控/审查）
1. 遵循 `global-agent-workflow.mdc`
2. 挂载并遵守 `agent_KB` 协议；读 `_meta/index`、`profile/preferences.md`、相关 `playbooks/`、对应 `projects/*.md`
3. 若当前仓在 `~/Documents/code/` 且尚未挂接 → **直接**跑 `hook-project.sh`
4. **识别到设计意图时自动**走 `global-ui-skills`（`npx ui-skills start` / `ui-skills-root`）
5. 按场景读对应 `SKILL.md`（本轮仅 1 个 L1 主流程 skill）
6. 覆盖自检：`python3 ~/Documents/code/skill/scripts/check-skill-coverage.py`
